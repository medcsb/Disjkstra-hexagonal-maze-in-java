package graph;

public interface ProcessedVertexes {
		
	public boolean isIn(Vertex v); //Checks wether or not a vertex is processed
	
	public void addVertex(Vertex v); //adds a vertex to the processed vertexes

}
